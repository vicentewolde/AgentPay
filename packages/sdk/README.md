# @agentpass/sdk

`issue()` / `verify()` / `revoke()` — core's crypto plus Soroban RPC against the
`agent_registry` contract. Lands in T7; the surfaces currently throw
`AgentPassError` with code `NotImplemented`.

```bash
pnpm --filter @agentpass/sdk test
```
