# @agentpass/core

Network-free primitives: typed errors, `did:stellar` derivation (T3) and
VC-JWT sign/verify (T4). Nothing in this package performs I/O.

```bash
pnpm --filter @agentpass/core test
```
