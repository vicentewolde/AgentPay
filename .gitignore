# node
node_modules/
dist/
*.tsbuildinfo

# rust
target/

# stellar cli local settings
.soroban/
.stellar/

# secrets — .env.example is the versioned template, .env.local never is
.env
.env.local
.env.*.local

# demo artefact from the README walkthrough
credential.jws

# apps/web's durable MandateVault (T27) — runtime data, not source
/data/

# os
.DS_Store
