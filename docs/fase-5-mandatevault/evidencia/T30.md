# Evidencia — T30 (`AgentPass.getRecord()`, identidad en la bitácora)

## 1. Forma real de `get_credential`, verificada contra el contrato desplegado

Antes de escribir `credRecordSchema`, una llamada de una sola vez contra
`AGENT_REGISTRY_CONTRACT_ID` real, para un hash ya anclado:

```
$ node scratch-inspect-cred.mjs ab4dfb10bb9d07b356ae9a1d4e4eabef8e8c608a8ec780b3f89cc2bfc394827b
{
  "expires_at": "1788623260n",
  "issued_at": "1788536887n",
  "issuer": "GCTTRJIYYRHAOYTEG2YWKYSBKIQOMX7RX7SAJXQAM3QY2J7J453BOZD6",
  "revoked": false,
  "subject": "GAK6E5E7L63ZYFZZZFXDTYVG6MVAKILSHI5FITGH5U4ORACEZQ4GFP2K"
}
```

Confirma: `issued_at`/`expires_at` son enteros de 64 bits en **segundos**
(no milisegundos), `issuer`/`subject` son direcciones Stellar (`G...`), no
DIDs.

## 2. `pnpm --filter @agentpass/sdk exec tsc --noEmit` y `pnpm typecheck` (monorepo)

```
(sin salida — limpio, en ambos casos)
```

## 3. `pnpm --filter @agentpass/sdk run test:integration` — contra testnet real

```
$ vitest run --config vitest.integration.config.ts

 RUN  v3.2.7 /Users/vicentewolde/dev/AgentPay/packages/sdk

 ✓ src/agentpass.integration.test.ts (3 tests) 18117ms
   ✓ the full cycle on live testnet > issues, verifies, revokes, and then refuses to verify  16958ms
   ✓ the full cycle on live testnet > refuses a validly signed credential that was never anchored  585ms

 Test Files  1 passed (1)
      Tests  3 passed (3)
```

Cubre, dentro del ciclo completo:
- `getRecord(issued.hash)` recién emitida: `issuer`/`subject` correctos,
  `revoked: false`, `expiresAt` coincide con `validUntil` truncado al
  segundo.
- `getRecord(issued.hash)` después de `revoke()`: `revoked: true`.
- `getRecord(signed.hash)` para un hash nunca anclado: `undefined`.

## 4. `pnpm test` y `pnpm build` — monorepo completo (sin regresiones)

```
Total: 74 (core) + 16 (sdk) + 17 (vault) + 44 (mandate) + 29 (cli)
     + 416 (agent) + 34 (scripts) = 630 tests, 0 failing

$ tsc -b
(sin salida — limpio)
```

## 5. Verificación en un navegador real (Claude Browser), contra el servidor local

Flujo completo: iniciar sesión → comprar → ver bitácora → revocar → ver
bitácora de nuevo, sin recargar la página entre pasos.

**Después de comprar:**

```
Cadena íntegra ✓ (2 registros)
credencial (en cadena): activa
mandato (en cadena): activa
#0 autorizada: b72b8120-0999-4eeb-a4a3-36627be34bdb — 0.0010000 USDC
#1 anclada: b72b8120-0999-4eeb-a4a3-36627be34bdb — pago
   089d6ccce47303b78c3c6c16d557473ea7d92e4075bddba9262ead55f7082fc1
   · ancla e6c34acb71fa6096f670bc896ac167537b2d06eeb42d110a2daef3d05794e9c2
   — on-chain: Active
```

**Después de revocar el Mandato (mismo botón "Ver bitácora", sin recargar
la página):**

```
mandato revocado: 4aead5efad7e398c005b1ac83def55d7b1b44c8066db5490347f9d39b500db7a
tx: 5478e1e42b4bfd7aabe58f0dd40647f507b8d758b8d5b3cb1d42ef448d87a8f6
credencial (en vivo): Active

Cadena íntegra ✓ (2 registros)
credencial (en cadena): activa
mandato (en cadena): revocada
```

`mandato (en cadena)` pasó de `activa` a `revocada` sin recargar la página
— la lectura es en vivo (`agentpass.getRecord()`, llamado de nuevo en cada
pedido), no un valor de cuando se inició la sesión. La credencial, nunca
revocada, se mantuvo `activa` en ambas lecturas — la independencia entre
las dos autoridades (T20/T23) sigue intacta.

Transacciones reales, verificables en Stellar Expert (testnet):
- Pago: `089d6ccce47303b78c3c6c16d557473ea7d92e4075bddba9262ead55f7082fc1`
- Anclaje del pago: `e6c34acb71fa6096f670bc896ac167537b2d06eeb42d110a2daef3d05794e9c2`
- Revocación del Mandato: `5478e1e42b4bfd7aabe58f0dd40647f507b8d758b8d5b3cb1d42ef448d87a8f6`
