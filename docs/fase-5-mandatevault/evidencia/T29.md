# Evidencia — T29 (superficie de consulta)

## 1. `pnpm test` — `packages/vault` (incluye los tests de `recordAnchor`)

```
$ vitest run

 RUN  v3.2.7 /Users/vicentewolde/dev/AgentPay/packages/vault

 ✓ src/vault.test.ts (17 tests) 16ms

 Test Files  1 passed (1)
      Tests  17 passed (17)
```

## 2. `pnpm typecheck`, `pnpm test`, `pnpm build` — monorepo completo

```
$ tsc -b && tsc -p tsconfig.scripts.json
(sin salida — limpio)

Total: 74 (core) + 16 (sdk) + 17 (vault) + 44 (mandate) + 29 (cli)
     + 416 (agent) + 34 (scripts) = 630 tests, 0 failing

$ tsc -b
(sin salida — limpio)
```

## 3. Verificación en un navegador real (Claude Browser), contra el servidor local

Servidor levantado vía `preview_start` (`.claude/launch.json` →
`agentpay-web`, puerto 8787), `data/mandate-vault.jsonl` borrado antes de
empezar.

**Paso 1 — Iniciar sesión.** Credencial y Mandato emitidos y anclados en
testnet (`credencial: 40166c04...`, `mandato: 75e0c980...`), estado
`Active`.

**Paso 2 — Comprar.** Pago x402 real ejecutado y asentado:

```
entendido: 1 x Swap Risk Quote (swap-risk-quote)
intent_id: 22b563f2-b5bd-4383-a3e4-0c5058b87170
total: 0.0010000 USDC
settled: true
tx: a9e353df24caae674c18e1b39c0d3016d037c47fbe82673d1ce61bc8d9bb817a
vault_anchor_hash: f34278bb4c476a9b3f0d503d2993078110a83f32acc93efa5b81aa72485c05bb
vault_anchor_tx: 39510bcd609e7643dba9374b6fa2e02773cd5df907b49a842386fc64265475be
```

**Paso 3 — La sección "Bitácora" se actualizó sola** (el frontend pide
`GET /api/session/vault` automáticamente después de cada compra, sin
segundo clic):

```
Cadena íntegra ✓ (2 registros)
#0 autorizada: 22b563f2-b5bd-4383-a3e4-0c5058b87170 — 0.0010000 USDC
#1 anclada: 22b563f2-b5bd-4383-a3e4-0c5058b87170 —
   pago a9e353df24caae674c18e1b39c0d3016d037c47fbe82673d1ce61bc8d9bb817a
   · ancla 39510bcd609e7643dba9374b6fa2e02773cd5df907b49a842386fc64265475be
   — on-chain: Active
```

`on-chain: Active` es una lectura en vivo de `agentpass.status(linkHash)` en
el momento de pedir la bitácora — no un valor guardado de cuando se ancló
(`V-10`).

Captura de la sección, tal como se vio en el navegador:

```
5 · BITÁCORA (MANDATEVAULT)
Cada decisión de autorización — aprobada o rechazada — y el anclaje
on-chain de cada pago real. La cadena se verifica al vuelo; el estado de
cada anclaje se lee en vivo del registro, no del archivo local.

[Ver bitácora]

Cadena íntegra ✓ (2 registros)
```

Transacciones reales, verificables en Stellar Expert (testnet):
- Pago: `a9e353df24caae674c18e1b39c0d3016d037c47fbe82673d1ce61bc8d9bb817a`
- Anclaje: `39510bcd609e7643dba9374b6fa2e02773cd5df907b49a842386fc64265475be`
