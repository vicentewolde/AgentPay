# Evidencia · T3 — Identidad derivable sin red

Fecha: 2026-09-01 · Commit: `2d9ec65`

## Tests de `@agentpass/core`

```
✓ src/errors.test.ts (2 tests) 2ms
✓ src/did.test.ts (21 tests) 42ms

Test Files  2 passed (2)
     Tests  23 passed (23)
```

Los dos tests que cubren el criterio de aceptación:

```
✓ recovers the original raw public key for 100 random keypairs
✓ preserves every byte for 100 random 32-byte keys, independently of Keypair
```

El primero usa `Keypair.random()` y compara contra `keypair.rawPublicKey()`.
El segundo genera 32 bytes con `node:crypto` y encodea con `StrKey` directamente,
sin pasar por `Keypair`. Si ambas rutas discreparan, el segundo lo detectaría.

## Entradas malformadas rechazadas (13 casos)

Cada caso se prueba contra `parseStellarDid` **y** `didToPublicKey`, y el test
verifica explícitamente que se lanzó una excepción — no basta con que no
devuelva el valor esperado.

| caso | código de error |
|---|---|
| string vacío | `InvalidDid` |
| pocos segmentos | `InvalidDid` |
| demasiados segmentos | `InvalidDid` |
| scheme equivocado (`urn:`) | `InvalidDid` |
| método equivocado (`did:key:`) | `InvalidDid` |
| red desconocida (`mainnet`) | `InvalidDid` |
| scheme en mayúsculas | `InvalidDid` |
| espacio inicial | `InvalidDid` |
| dirección vacía | `InvalidStellarAddress` |
| **una semilla secreta `S...`** | `InvalidStellarAddress` |
| **un contract id `C...`** | `InvalidStellarAddress` |
| dirección truncada | `InvalidStellarAddress` |
| checksum mutado | `InvalidStellarAddress` |

## Mutation testing — ¿los tests tienen dientes?

Se rompió la implementación a propósito y se verificó que los tests fallaran.

**Mutación 1** — devolver una llave truncada
(`decodeEd25519PublicKey(address).slice(0, 31)`):

```
FAIL  src/did.test.ts > recovers the original raw public key for 100 random keypairs
FAIL  src/did.test.ts > preserves every byte for 100 random 32-byte keys, independently of Keypair
FAIL  src/did.test.ts > performs no I/O — the module never reaches the network
```

**Mutación 2** — aceptar cualquier dirección no vacía
(`isStellarAddress` devuelve `value.length > 0`):

```
FAIL  src/did.test.ts > rejects a secret seed with a typed error, never undefined
FAIL  src/did.test.ts > rejects a contract id with a typed error, never undefined
FAIL  src/did.test.ts > rejects a truncated address with a typed error, never undefined
```

Implementación restaurada: `Tests  23 passed (23)`.

## Estabilidad

Se encontró y corrigió un flake en el propio test: el caso "checksum mutado"
hacía `address.slice(0,-1) + "A"`, que no muta nada si el último carácter ya era
`A` — habría fallado ~1 de cada 32 corridas. Ahora la mutación es determinista.

Cinco corridas consecutivas tras la corrección:

```
Tests  23 passed (23)
Tests  23 passed (23)
Tests  23 passed (23)
Tests  23 passed (23)
Tests  23 passed (23)
```

## Auditoría del historial antes del primer push

```
=== ¿alguna clave secreta (S...) en TODO el historial? ===
(ninguna)
=== ¿.env.local alguna vez commiteado? ===
0 — nunca entró al historial
```
