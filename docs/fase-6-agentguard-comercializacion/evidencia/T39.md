# T39 — evidencia

Persistencia de sesión. Rama `cc/t39-session-persistence`, desde
`c3bd052` (T38 + addendum de delegación).

---

## 1. `pnpm typecheck` — limpio

```
$ pnpm typecheck
> tsc -b && tsc -p tsconfig.scripts.json
```

## 2. `pnpm build` — limpio

```
$ pnpm build
> tsc -b
```

## 3. `pnpm test` — 773 en verde, de 759

```
packages/core       80 passed (80)
packages/sdk        34 passed (34)
packages/vault      17 passed (17)
packages/tenancy     9 passed (9)
packages/directory  25 passed (25)    <- sin cambio: los métodos nuevos solo tienen test de integración
packages/cli        29 passed (29)
packages/mandate    61 passed (61)
apps/agent         421 passed (421)
apps/web            63 passed (63)    <- +14 sobre los 49 de T38 (16 nuevos, 2 retirados con walletTenantId)
raíz                 34 passed (34)
```

Suma verificada con el propio resultado de `pnpm test`, no de memoria:
`80+34+17+9+25+29+61+421+63+34 = 773`.

`apps/web` en detalle:

```
✓ src/wallet-session.test.ts (19 tests)
✓ src/session-rehydration.test.ts (8 tests)   <- nuevo
✓ src/shared-identity.test.ts (8 tests)        <- nuevo
✓ src/env.test.ts (19 tests)
✓ src/session-documents.test.ts (9 tests)
```

## 4. `packages/directory` — test de integración, 19 en verde contra Postgres real

```
$ pnpm --filter @agentpay/directory run test:integration
 ✓ src/directory.integration.test.ts (19 tests) 111025ms
   ...
   ✓ finds a shared agent by its Stellar address — the lookup a bootstrap step needs to be idempotent
   ✓ finds the latest credential for a tenant, even before F4 gives it its own agent
   ✓ finds the latest mandate for a tenant regardless of revoked or expired status
   ...
 Test Files  1 passed (1)
      Tests  19 passed (19)
```

Confirma que la migración de `tenant_id` en `directory_credentials` corrió
limpia contra la base real (la misma que usa el sitio en producción), sin
romper ninguna fila existente.

---

## 5. Ningún punto de autorización tocado

```
$ git diff --stat c3bd052..HEAD -- apps/agent contracts
(sin salida)
```

`checkMandate`, `checkScope`, `checkDailyLimit`, `policy_rail`,
`agent_registry` — byte por byte iguales.

---

## 6. Verificación manual contra testnet real

Cinco corridas con un script descartable (nunca commiteado), usando
`ISSUER_SECRET_KEY` —ya fondeada y ya registrada como emisor— como la
"wallet conectada", firmando con SEP-0053 real
(`signStellarMessage`) y transacciones Stellar reales.

### 6.1 Primera conexión — emite de verdad

```
wallet address: GCTTRJIYYRHAOYTEG2YWKYSBKIQOMX7RX7SAJXQAM3QY2J7J453BOZD6
1. challenge: 200 b5a95ee8-5f5c-42d2-b584-ae450f2feeb3
2. verify: 200 { ok: true, address: 'GCTT...' }
   cookie: agentpay_sid=ptn_01M26SGHZ4BM2316DC8F2X9MZW:01M26SGMVHMYE4E86WAT5DGNQV
3. session/start #1: 200 { ok: true, pending: 'wallet-consent',
   credentialHash: 'ac5a3f0a...9802a',
   challengeMessage: 'AgentPay Mandate\nAgent: did:stellar:testnet:...' }
4. wallet-consent: 200 { requestId: '01984201-...', xdrLen: 1120 }
5. wallet-anchor: 200 { ok: true,
   credentialHash: 'ac5a3f0a...9802a',
   mandateHash: '98f1ac59...627419',
   agentStatus: 'Active', policyRail: 'CCGAGRLV...4G3I' }
```

El cookie ya es un **tenant id real** del directorio
(`ptn_<ULID>:<ULID>`), no `sha256(dirección)` — confirma `C-25`/`D4`
aplicado.

### 6.2 Segunda llamada, mismo proceso — rehidrata

```
6. session/start #2 (same wallet, same tenant cookie): 200 {
   ok: true,
   credentialHash: 'ac5a3f0a...9802a',   <- idéntico
   mandateHash: '98f1ac59...627419',      <- idéntico
   ... }

✅ PASSED: second /api/session/start rehydrated the exact same
credential and mandate.
```

Nada de `pending: "wallet-consent"` — no hubo emisión ni anclaje nuevos.

### 6.3 Proceso muerto, proceso nuevo — rehidrata desde Postgres solo

Se detuvo el servidor (`preview_stop`) y se levantó uno nuevo
(`preview_start`) — `sessions`, `pendingWalletSessions` y
`walletAddressBySession` en memoria, todos vacíos.

```
wallet address: GCTTRJIYYRHAOYTEG2YWKYSBKIQOMX7RX7SAJXQAM3QY2J7J453BOZD6
verify: 200 { ok: true, address: 'GCTT...' }
   cookie: agentpay_sid=ptn_01M26SGHZ4BM2316DC8F2X9MZW:01M26SGMVHMYE4E86WAT5DGNQV
✅ same tenant id found across the restart — ensureVisitorTenant's
lookup, not a fresh create.

session/start (fresh process, no in-memory state at all): 200 {
   ok: true,
   credentialHash: 'ac5a3f0a...9802a',
   mandateHash: '98f1ac59...627419',
   ... }

✅ PASSED: a brand new server process rehydrated the exact same
credential and mandate from Postgres alone.
```

Mismo tenant id (`ensureVisitorTenant` encontró la fila, no creó una
nueva) y mismos hashes exactos que antes de matar el proceso.

### 6.4 Una compra real sobre una sesión puramente rehidratada

```
session/start: 200 { pending: undefined, credentialHash: 'ac5a3f0a...',
mandateHash: '98f1ac59...' }
vault (history) on a purely rehydrated session: 200 { ok: true,
chainOk: true, identityCount: 2, recordCount: 0 }
✅ history reads correctly on a rehydrated session

buy: 200 { "ok": true, "steps": [
  { "label": "entendido", "value": "1 x Swap Risk Quote (swap-risk-quote)" },
  { "label": "total", "value": "0.0010000 USDC" },
  { "label": "pagador", "value": "CCGAGRLV...4G3I (policy_rail, límites on-chain)" },
  { "label": "settled", "value": "true" },
  { "label": "tx", "value": "4fb79e4f40cfab4251992b3e216001927e004a0083ce43820146a2a7c7a3da15" },
  { "label": "explorer", "value": "https://stellar.expert/explorer/testnet/tx/4fb79e4f..." },
  { "label": "vault_anchor_hash", "value": "06f96f4c2b79230a291000eeab5bf467dc5c690560e7d148c30d450212d96dea" },
  { "label": "vault_anchor_tx", "value": "4aa1c011c224f1a5b53936ae57ba27da4f6a0b52e9d21b0abf7f92d15f045b34" }
]}

✅ PASSED: a purely rehydrated session (checkMandate + checkScope
against the persisted, re-parsed mandate) completed a real payment.
```

Prueba que el documento reconstruido —reparseado desde la columna `json`
con `agentPayMandateSchema.safeParse`, no el objeto original en
memoria— sigue siendo un `AgentPayMandate` que `checkMandate`/`checkScope`
aceptan, y que `policy_rail` liquida de verdad.

### 6.5 Revocar corta la rehidratación; la renovación encadena `supersedesId`

```
pre-revoke mandateHash: 98f1ac59...627419 (should rehydrate,
pending undefined): undefined
revoke: 200 { ok: true, pending: 'wallet-signature', requestId: '...', xdr: '...' }
wallet-revoke-submit: 200 { ok: true, mandateHash: '98f1ac59...',
revokeTx: '166c8a8c937e...', credentialStatus: 'Active' }

session/start after revoke: 200 { pending: 'wallet-consent',
credentialHash: 'c0ab5345...', mandateHash: undefined }

✅ PASSED: after revoking, the same wallet is asked to sign a fresh
Mandate — the revoked one is never rehydrated.

renewal anchor: 200 { credentialHash: 'c0ab5345...',
mandateHash: 'b1b45784...' }
✅ renewal produced a new mandate, distinct from the revoked one —
old: 98f1ac59...627419 new: b1b45784...bf5ab3
```

Confirmado directamente contra la base, no solo por inferencia del
comportamiento HTTP:

```
old mandate: id= mdt_01M26SH42F8YVAEWEDBMF0WHJX revokedAt= 2026-09-10T23:15:43.358Z
new mandate: id= mdt_01M26SPDZQWHCDCQ83APSZRRAC supersedesId= mdt_01M26SH42F8YVAEWEDBMF0WHJX

✅ PASSED: the renewal correctly chains supersedesId to the revoked
mandate it replaced.
```

---

## 7. Nota sobre los scripts de verificación

Los cinco scripts usados en la sección 6 (`tmp-t39-*.ts`) se escribieron,
corrieron y borraron dentro de la misma sesión — nunca se commitearon, el
mismo patrón que `C-18`/T36 ya estableció para verificar rutas HTTP contra
testnet real en vez de mockear `AgentPass`/Stellar. Lo que sí queda como
test permanente es la lógica pura que gobierna la decisión de rehidratar
(`session-rehydration.test.ts`) y el bootstrap idempotente
(`shared-identity.test.ts`).
